<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <% @questions.each do |question| %>
        <%= erb :'/partials/_questions_all', locals: {question: question} %>
      <% end %>
    </div>
  </div>
</div>
